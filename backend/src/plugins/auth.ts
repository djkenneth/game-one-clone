/**
 * @deprecated Import from '../middleware/auth.middleware' instead.
 * Kept for backward compatibility with existing route/controller imports.
 */
export { authMiddleware, isAdminMiddleware, type AuthEnv } from '../middleware/auth.middleware'
